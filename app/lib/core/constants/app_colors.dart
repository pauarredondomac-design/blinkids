import 'package:flutter/material.dart';

abstract class AppColors {
  // Primarios
  static const primary = Color(0xFF4CAF50);
  static const primaryDark = Color(0xFF2E7D32);
  static const primaryLight = Color(0xFF81C784);

  // Secundarios (moneda dorada)
  static const secondary = Color(0xFFFFB300);
  static const secondaryDark = Color(0xFFFF8F00);
  static const secondaryLight = Color(0xFFFFE082);

  // Acento mágico
  static const accent = Color(0xFF7C4DFF);

  // Fondos
  static const background = Color(0xFFF1F8E9);
  static const surface = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // Texto
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF616161);
  static const textHint = Color(0xFF9E9E9E);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const textOnSecondary = Color(0xFF1A1A2E);

  // Estados
  static const success = Color(0xFF43A047);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFB300);
  static const info = Color(0xFF1976D2);

  // Mundos
  static const forestGreen = Color(0xFF2E7D32);
  static const spaceBlue = Color(0xFF1A237E);
  static const spacePurple = Color(0xFF4A148C);
  static const cityNeon = Color(0xFF00BCD4);
  static const cityCyan = Color(0xFF006064);

  // UI de juego
  static const coinGold = Color(0xFFFFD700);
  static const xpPurple = Color(0xFF9C27B0);
  static const border = Color(0xFFE0E0E0);
  static const divider = Color(0xFFEEEEEE);
  static const overlay = Color(0x80000000);
  static const overlayLight = Color(0x40000000);

  // Categorías de la bolsa
  static const categoryAhorro = Color(0xFF2196F3);
  static const categoryInversion = Color(0xFF4CAF50);
  static const categoryEmergencia = Color(0xFFFF5722);
  static const categoryGastos = Color(0xFFFF9800);
  static const categoryMetas = Color(0xFF9C27B0);
}
